export interface HeaderProps {
    isSearch: boolean;
    setIsSearch: (value: boolean) => void;
}

export interface MainInfoProps {
    isSearch: boolean;
    setIsSearch: (value: boolean) => void;
}